var today = moment().format ("MMM Do, YYYY, h:mm:ss a");
$("#currentDay").text(today);
var hour = moment().format("h" +"a" );
console.log (moment().hours());

var a= moment().startOf('day').fromNow();
console.log(a);
//function timecheck() { 
 //   if (hour = moment(h))
   // then }
    
   // else if (hour < moment(h)){}  
  // 
   // else()
//

